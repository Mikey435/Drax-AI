services:
    - type: web
    name: drax-ai
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
            - key: GROQ_API_KEY
        sync: false
